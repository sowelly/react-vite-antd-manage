import {observer} from 'mobx-react-lite';
import  './index.less'

function ContactPage() {


    return (
        <div>
            ContactPage

        </div>
    );
}

export default observer(ContactPage); // 使用 observer 包裹组件